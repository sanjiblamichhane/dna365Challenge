//fileName: w1.h
#include <iostream>
using namespace std;

class Pair{
	public:
		Pair(int, int); //constructor
			// example usage: p.Pair(a, b);
			// p is an object that takes two integer values a and b
			// populates public data members
			// returns no values

		int sum(); // a method
			// example usage: p.sum(a, b);
			// has no argument
			// adds the two member variables
			// retuns the sum

		//date members
		int a, b;
};

//constructor
Pair::Pair(int x, int y)
{
	// constructor implementation 
	x = a;
	y = b;
}

// start method implementation
int
Pair::sum()
{
	int sum;
	int a, b;
	cout<<"++ Start Sum Method ++" <<endl;
	sum == 	
	if(p.a + p.b == p.sum())
	{
		cout<<" ++ Sum Successful! ++ "<<endl;
		return 0;
	}
	else 
	{
		cout<<" ++ Sum returns 
	}
	
	sum = a + b;
	cout<<"++ End Sum Method ++" <<endl;
	return sum;
}
